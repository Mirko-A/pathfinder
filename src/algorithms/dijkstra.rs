use crate::algorithms::grid;
use crate::algorithms::PathfindingAlgorithm;

#[allow(dead_code)]

pub struct Dijkstra;

impl PathfindingAlgorithm for Dijkstra {
    fn find_shortest_path(start: grid::Point, end: grid::Point, grid: &grid::Grid) {
        unimplemented!();
    }
}
